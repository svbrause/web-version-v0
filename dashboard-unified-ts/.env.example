# Environment Variables for Dashboard
# NOTE: Most environment variables are optional!
# The app uses hardcoded defaults that point to the secure backend API.
# All API calls go through: https://ponce-patient-backend.vercel.app
# No API keys or sensitive data is needed in this frontend project.

# PostHog Analytics (Optional)
# Get your PostHog project API key from: https://posthog.com/settings/project
VITE_POSTHOG_KEY=your_posthog_project_api_key_here

# PostHog Host (Optional - defaults to https://us.i.posthog.com)
# Only needed if using a self-hosted PostHog instance
# VITE_POSTHOG_HOST=https://us.i.posthog.com
